/**
 * Maintenance photos use {@link PhotoUploader} with report layout grid classes.
 * Re-export for a stable module path (`components/report/PhotoGrid`).
 */
export { PhotoUploader as MaintenancePhotoGrid } from '@/components/maintenance/PhotoUploader'
